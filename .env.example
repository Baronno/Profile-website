# Copy this file to `.env` (do NOT commit your real key).
# Add your OpenAI API key here. Vite will expose this variable to the client bundle.
# SECURITY: For production, do NOT expose your API key in a client-side build. Use a server-side proxy instead.
VITE_OPENAI_API_KEY=your_openai_api_key_here
